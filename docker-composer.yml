version: '3'
services:
  spring-boot-prod-docker-app:
    image: beksultancs/docker-app
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '80:5000'
    environment:
      - "SPRING_PROFILES_ACTIVE=prod"